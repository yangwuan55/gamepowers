# ECS 原则

## 原则
- 实体只做标识，不承载业务逻辑。
- 组件仅保存数据，避免行为代码。
- 系统按职责处理组件集合。
- 系统调度顺序必须稳定可复现。

## 系统边界
- 输入系统：只采集输入，不直接修改战斗结果。
- AI 系统：只生成决策意图，不直接写血量或奖励。
- 技能系统：负责技能触发与冷却，不负责伤害结算。
- 伤害系统：负责伤害与状态变化结算，不负责渲染。
- 同步系统：负责状态广播与序列化，不负责业务判定。
